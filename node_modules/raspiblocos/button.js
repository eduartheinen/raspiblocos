var onoff = require('onoff').Gpio;

var button = function(number) {
  var button = new onoff(number, 'in', 'falling');

  return {

    setListener: function(callback) {
      button.watch(callback);
    },

    unexport: function() {
      button.unexport();
    }
  }
}

module.exports = button;
