var Q = require('q');

module.exports = function wait(time) {
  var deferred = Q.defer();
  this.setTimeout(function() {
    deferred.resolve('waited ' + time + 'ms');
  }, time);

  return deferred.promise;
};
